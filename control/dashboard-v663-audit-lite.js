'use strict';
(()=>{
if(window.ONE_SHOT_AUDIT_LITE_V663)return;
window.ONE_SHOT_AUDIT_LITE_V663=true;
const $=id=>document.getElementById(id);
const norm=v=>String(v??'').trim();
let CFG=null,READY=new Map(),timer=0,loading=false;
async function config(){if(CFG)return CFG;const src=[...document.scripts].map(s=>s.src).find(s=>/\/control\/app\.js/.test(s))||'app.js';const text=await fetch(src,{cache:'force-cache'}).then(r=>r.text()),url=(text.match(/SUPA_URL='([^']+)'/)||[])[1],key=(text.match(/SUPA_KEY='([^']+)'/)||[])[1];if(!url||!key)throw new Error('Configuración central no disponible');return CFG={url,key}}
async function getReadiness(){const c=await config(),r=await fetch(`${c.url}/rest/v1/control_evidence_readiness?select=code,audit_state,audit_score,blocking_reasons,ready_for_export,app_review_status,import_id,source_file,imported_at&order=code`,{cache:'no-store',headers:{apikey:c.key,Authorization:'Bearer '+c.key}});if(!r.ok)throw new Error(`Readiness HTTP ${r.status}`);return r.json()}
const color=s=>s==='ACEPTADA'?'#15803d':s==='VALIDADA_APP'?'#22c55e':s==='LISTA'?'#2563eb':s==='BLOQUEADA'?'#dc2626':'#f59e0b';
const label=s=>s==='ACEPTADA'?'Aceptada en CONTROL':s==='VALIDADA_APP'?'Validada en app':s==='LISTA'?'Lista para salida':s==='BLOQUEADA'?'Bloqueada':'En revisión';
function patchKpi(){if(!$('kPending'))return;const rows=[...READY.values()];$('kPending').textContent=rows.filter(r=>['BLOQUEADA','EN_REVISION'].includes(r.audit_state)).length;const s=$('kPending').nextElementSibling;if(s)s.textContent='Por revisar CONTROL';let card=$('kAppValidatedCard');if(!card){card=document.createElement('article');card.id='kAppValidatedCard';card.innerHTML='<strong id="kAppValidated">0</strong><span>Validadas en app</span>';document.querySelector('.kpis')?.insertBefore(card,$('kPending')?.closest('article')||null)}if($('kAppValidated'))$('kAppValidated').textContent=rows.filter(r=>r.audit_state==='VALIDADA_APP').length}
function patchLegend(){const host=document.querySelector('.legend');if(!host||host.dataset.v665)return;host.dataset.v665='1';host.querySelectorAll('.v663Legend').forEach(x=>x.remove());const hint=$('mapHint'),items=[['#dc2626','Bloqueada'],['#f59e0b','En revisión'],['#2563eb','Lista'],['#22c55e','Validada app'],['#15803d','Aceptada CONTROL']];items.forEach(([c,t])=>{const x=document.createElement('span');x.className='v663Legend';x.innerHTML=`<i style="display:inline-block;width:9px;height:9px;border-radius:50%;background:${c};margin-right:5px"></i>${t}`;host.insertBefore(x,hint||null)})}
function patchMarkers(){document.querySelectorAll('.nm-marker[data-code]').forEach(m=>{const r=READY.get(m.dataset.code);if(!r)return;m.style.setProperty('--mc',color(r.audit_state));m.dataset.auditState=r.audit_state;m.title=`${m.dataset.code} · ${label(r.audit_state)}${r.blocking_reasons?.length?' · Pendiente CONTROL: '+r.blocking_reasons.join(', '):''}`})}
function patchRows(){document.querySelectorAll('#rows .row[data-code]').forEach(row=>{const r=READY.get(row.dataset.code);if(!r)return;let tag=row.querySelector('.v663State');if(!tag){tag=document.createElement('em');tag.className='v663State';tag.style.cssText='display:inline-block;margin-top:4px;font-style:normal;font-size:8px;font-weight:900;border-radius:999px;padding:3px 6px';row.appendChild(tag)}tag.textContent=label(r.audit_state);tag.style.background=r.audit_state==='BLOQUEADA'?'#fee2e2':r.audit_state==='ACEPTADA'?'#dcfce7':r.audit_state==='VALIDADA_APP'?'#e7fbe9':r.audit_state==='LISTA'?'#dbeafe':'#fef3c7';tag.style.color=r.audit_state==='BLOQUEADA'?'#991b1b':r.audit_state==='ACEPTADA'?'#166534':r.audit_state==='VALIDADA_APP'?'#15803d':r.audit_state==='LISTA'?'#1e40af':'#92400e'})}
function modalCode(){return norm($('gaTitle')?.textContent)}
function patchModal(){const modal=$('geoAuditModal');if(!modal?.classList.contains('open'))return;const r=READY.get(modalCode());if(!r)return;const tabs=modal.querySelector('.gaTabs');let bar=$('v663AuditBar');if(!bar&&tabs){bar=document.createElement('div');bar.id='v663AuditBar';bar.style.cssText='display:flex;align-items:center;gap:10px;padding:8px 16px;background:#fff;border-bottom:1px solid #dce5f0;font-size:10px;flex-wrap:wrap';tabs.insertAdjacentElement('afterend',bar)}if(bar){const detail=r.audit_state==='VALIDADA_APP'?'ONE SHOT ya la dejó corregida. Revisión CONTROL opcional.' : (r.blocking_reasons||[]).length?'Pendiente CONTROL: '+r.blocking_reasons.join(' · '):'Sin bloqueos';bar.innerHTML=`<b style="color:${color(r.audit_state)}">${label(r.audit_state)} · ${r.audit_score}/100</b><span>${detail}</span>`}const btn=modal.querySelector('#gaDeliveryReview [data-delivery-act="accept"]');if(btn){btn.disabled=!r.ready_for_export;btn.textContent=r.ready_for_export?'✓ Aceptar evidencia para salida':`🔒 CONTROL: ${(r.blocking_reasons||[]).slice(0,4).join(' · ')||'validaciones'}`}}
function patch(){patchKpi();patchLegend();patchMarkers();patchRows();patchModal();window.dispatchEvent(new CustomEvent('oneshot:readiness-patched',{detail:{rows:[...READY.values()]}}))}
async function refresh(){if(loading)return;loading=true;try{const rows=await getReadiness();READY=new Map(rows.map(r=>[r.code,r]));patch()}catch(e){console.warn('[ONE SHOT audit-lite]',e)}finally{loading=false}}
function schedule(ms=80,refetch=false){clearTimeout(timer);timer=setTimeout(async()=>{if(refetch)await refresh();else patch()},ms)}
function bind(){$('reloadBtn')?.addEventListener('click',()=>schedule(700,true));['fProcess','fRegion','fDistrict','fType','fParty','fStatus','fImport'].forEach(id=>$(id)?.addEventListener('change',()=>schedule(120,false)));$('fSearch')?.addEventListener('input',()=>schedule(300,false));window.addEventListener('oneshot:import-complete',()=>schedule(500,true));document.addEventListener('click',e=>{if(e.target.closest('#previewAuditBtn,#previewOpen,#previewImg,[data-ex-code],#rows .row[data-code]'))schedule(350,false);if(e.target.closest('#gaSave,[data-delivery-act="good"],[data-delivery-act="fix"],[data-delivery-act="accept"],#gaUseGps,#gaRefreshGeo'))schedule(650,true)},true)}
async function boot(){bind();await refresh();setTimeout(patch,350);setTimeout(patch,1200);console.info('[ONE SHOT CONTROL] audit-lite v6.65 · valida estado app')}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();
